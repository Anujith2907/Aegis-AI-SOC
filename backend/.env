PORT=8000
MONGODB_URI=mongodb://localhost:27017/cyberguard
JWT_SECRET=cyberguard-dev-secret-key-1234567890
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Generative AI (RAG)
# If empty, the backend will auto-fallback to simulated responses so the UI works without API keys!
GROQ_API_KEY=
LLM_MODEL=llama3-8b-8192

# Email Alerts
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
